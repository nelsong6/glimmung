apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.serviceAccount }}
  namespace: {{ .Values.namespace }}
  annotations:
    # Wired in infra-bootstrap/tofu/aks.tf via the k8s_apps for_each — the
    # federated credential subject is system:serviceaccount:glimmung:infra-shared.
    azure.workload.identity/client-id: "44b0ff37-b886-4101-ab19-23388ead9c0b"
  labels:
    azure.workload.identity/use: "true"
